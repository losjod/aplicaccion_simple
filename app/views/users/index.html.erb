<% provide(:title, 'All users') %>
<h1>All users</h1>
<% @users = User.paginate(:page => params[:page], :per_page => 15) %>

<%= will_paginate @users %>
<ul class="users">

  <%= render @users %>

</ul>
<%= will_paginate @users %>
